version: '2'
services:
  web: &web
    tty: true
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    command: ./vendor/bin/phpunit --configuration ./phpunit.xml
    ports:
      - "8000:8000"
